language: java

jdk:
  - openjdk8

services:
  - postgresql
addons:
  postgresql: "9.4"

caches:
  - gradle
  - gradlewrapper

before_script:
  - psql -c 'create database authentication_ntthuat;' -U postgres

script:
  - ./gradlew build